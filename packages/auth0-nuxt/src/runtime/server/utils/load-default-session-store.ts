/**
 * Unless configured otherwise, the default session store is expected to be undefined as the SDK relies on stateless sessions in that case.
 * This function is a placeholder to ensure that the module can be imported without errors when no custom session store is provided.
 * @returns 
 */
export default function loadDefaultSessionStore() {
    return undefined;
}